#pragma once

{%- set messages = data["Messages"] -%}
{%- set board = data["Board"] %}

#include <stdint.h>

typedef struct
{
{%- for message in messages -%}
    uint64_t {{message}};
{% endfor %}
} {{board}}_tx_struct;
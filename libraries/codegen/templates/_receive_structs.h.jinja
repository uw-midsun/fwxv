#pragma once

{% set board = data["Board"] -%}
{% set signals = data["Signals"] -%}

#include <stdint.h>

typedef struct {
    {% for (signal, length) in signals -%}
        {{length}} {{signal}};
    {% endfor -%}
} {{board}}_receive_struct;
#pragma once

{% set board = data["Board"] -%}
{% set signals = data["Signals"] -%}

#include <stdint.h>

typedef struct {
    {%- for signal, length, message in signals -%}
        {{length}} {{message}}_{{signal}};
    {% endfor -%}
} {{board}}_receive_struct;

